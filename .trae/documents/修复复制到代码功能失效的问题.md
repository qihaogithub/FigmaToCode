我将修复点击“复制到代码”按钮后无法复制的问题。这似乎是因为后端返回的消息对象中，代码内容的键名出现了异常（可能是本地化导致的中文键名），导致前端无法正确获取代码内容。

**修复计划：**

1. **修改前端消息处理 (`apps/plugin/ui-src/App.tsx`)**：

   * 增加对异常键名（如“代码”）的兼容支持，确保能正确提取代码内容。

   * 增加详细的调试日志，以便进一步排查。

   * 优化 `copyResolver` 的处理逻辑，确保即使获取代码失败，也能正常结束 loading 状态并给出反馈。

2. **增强后端消息发送 (`apps/plugin/plugin-src/code.ts`)**：

   * 在发送消息前打印即将发送的数据结构，用于确认后端逻辑的正确性。

3. **验证**：

   * 完成修改后，请重新尝试点击“复制到代码”，并观察控制台日志以及剪贴板内容。

